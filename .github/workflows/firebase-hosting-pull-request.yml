# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7' # Not needed with a .ruby-version file
          bundler-cache: true # runs 'bundle install' and caches installed gems automatically
      - uses: actions/cache@v3
        with: # cache built images
          path: images
          key: ${{ runner.os }}-images-${{ hashFiles('**/_original_images/*') }}
          restore-keys: |
            ${{ runner.os }}-images-
      - run: npm ci && npm run build-staging
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BLACKPEPPERSWING }}'
          target: staging
          channelId: live
          projectId: blackpepperswing
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
